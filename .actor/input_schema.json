{
    "$schema": "https://apify.com/schemas/v1/input.ide.json",
    "title": "Air Care Benchmark Processor",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "search_term": {
            "title": "Search Term",
            "type": "string",
            "description": "Término de búsqueda para consultar en Walmart (ej. plug in diffuser)",
            "editor": "textfield",
            "default": "plug in diffuser starter kit"
        },
        "max_pages": {
            "title": "Máximo de Páginas",
            "type": "integer",
            "description": "Número de páginas de resultados que revisará el scraper de Walmart",
            "default": 3
        },
        "max_items": {
            "title": "Max Items",
            "type": "integer",
            "description": "Número máximo de productos brutos a extraer de Walmart",
            "editor": "number",
            "default": 100
        }
    },
    "required": ["search_term"]
}
