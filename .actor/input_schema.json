{
    "$schema": "https://apify.com/schemas/v1/input.ide.json",
    "title": "Air Care Benchmark Processor",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "raw_dataset": {
            "title": "Raw Scraped Dataset",
            "type": "array",
            "description": "List of raw products scraped from Walmart with fields: productId, productTitle, productUrl, reviewCount, rating, seller, image",
            "editor": "json"
        }
    },
    "required": []
}
